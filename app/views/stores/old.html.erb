<div class="slider">
    <ul class="frames">
        <li id="one" class="slide">
          <%= image_tag(@store.image(:medium), class: "img-responsive") %>
            <nav>
                <a class="prev" href="#five">&larr;</a>
                <a class="next" href="#two">&rarr;</a>
            </nav>
        </li>
        <li id="two" class="slide">
          <%= image_tag(@store.image2(:medium), class: "img-responsive") %>
            <nav>
                <a class="prev" href="#one">&larr;</a>
                <a class="next" href="#three">&rarr;</a>
            </nav>
        </li>
        <li id="three" class="slide">
          <%= image_tag(@store.image3(:medium), class: "img-responsive") %>
            <nav>
                <a class="prev" href="#two">&larr;</a>
                <a class="next" href="#four">&rarr;</a>
            </nav>
        </li>
        <li id="four" class="slide">
          <%= image_tag(@store.image4(:medium), class: "img-responsive") %>
            <nav>
                <a class="prev" href="#three">&larr;</a>
                <a class="next" href="#five">&rarr;</a>
            </nav>
        </li>
        <li class="quicknav">
            <ul>
                <li><a href="#one"></a></li>
                <li><a href="#two"></a></li>
                <li><a href="#three"></a></li>
                <li><a href="#four"></a></li>
                <li><a href="#five"></a></li>
            </ul>
        </li>
    </ul>
</div>

<br>
<br>
<br>
<br>

<h2> Company Name: <%= @store.user.company_name %> </h2>
<h3> Headline: <%= @store.caption %> </h3>
<p> Zipcode: <%= @store.zipcode %> </p>
<p> Long Description: <%= @store.long_description %> </p>

<div class="lightbox">
<button class="back">Back</button>

<div class="picture-container">


</div>
<button class="forward">Forward</button>

</div>



<div class="picture pic1"><%= image_tag(@store.image(:medium), class: "img-responsive") %></div>
<div class="picture pic2"><%= image_tag(@store.image2(:medium), class: "img-responsive") %></div>
<div class="picture pic3"><%= image_tag(@store.image3(:medium), class: "img-responsive") %></div>
<div class="picture pic4"><%= image_tag(@store.image4(:medium), class: "img-responsive") %></div>





  <% if @store.user.id == current_user.id %>

  <button><%= link_to 'Edit Your Store', edit_store_path(@store) %></button>


  <button><%= link_to "Delete", store_path(@store), method: :delete, data: {confirm: "Are you sure you  want to delete this store?"}%></button>

  <% end %>

  <!-- Display Add Comment -->

    <div class="comment-form col-sm-11">
      <%= form_for [@store, @store.comments.new] do |f| %>
      <%= f.text_field :content, placeholder: 'Add'%>
      <%= f.submit 'Add'%>
    </div>
    <div>
        <% if @store.comments %>
          <% @store.comments.each do |comment| %>
        <!-- Display Actual Comment -->

         <div class="comment-content">
          <%= comment.content %>
          <% if comment.user == current_user %>
          <button><%= link_to "Delete This Comment",  store_comment_path(@store, comment), method: :delete, data: { confirm: "Are you sure?" } %></button>
          <% end %>
        </div>

    </div>
    <% end %>
    <% end %>

    <% end %>
